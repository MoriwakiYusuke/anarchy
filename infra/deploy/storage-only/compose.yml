# storage-only — storage×10
#
# ⚠️ このファイルは gen.py が生成する。手で編集せず、引数を変えて再生成すること:
#   ./gen.py storage-only --storage 10 --capacity 2G
#
# 設計の根拠は gen.py 冒頭と docs/operations/deployment-multi-provider.md §5 を参照。

services:
  tor:
    build:
      context: ../../docker/tor
    image: anarchy/tor:local
    restart: unless-stopped
    volumes:
      - tor-data:/var/lib/tor
      - ./torrc:/etc/tor/torrc:ro
    healthcheck:
      test: ["CMD", "sh", "-c", "nc -z 127.0.0.1 9050"]
      interval: 5s
      timeout: 2s
      retries: 24
      start_period: 10s

  storage-1:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3030
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3030
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage1-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-1.toml:/etc/anarchy/storage.toml:ro

  storage-2:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3031
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3031
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage2-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-2.toml:/etc/anarchy/storage.toml:ro

  storage-3:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3032
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3032
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage3-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-3.toml:/etc/anarchy/storage.toml:ro

  storage-4:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3033
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3033
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage4-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-4.toml:/etc/anarchy/storage.toml:ro

  storage-5:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3034
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3034
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage5-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-5.toml:/etc/anarchy/storage.toml:ro

  storage-6:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3035
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3035
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage6-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-6.toml:/etc/anarchy/storage.toml:ro

  storage-7:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3036
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3036
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage7-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-7.toml:/etc/anarchy/storage.toml:ro

  storage-8:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3037
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3037
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage8-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-8.toml:/etc/anarchy/storage.toml:ro

  storage-9:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3038
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3038
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage9-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-9.toml:/etc/anarchy/storage.toml:ro

  storage-10:
    image: ${ANARCHY_STORAGE_IMAGE:-ghcr.io/moriwakiyusuke/anarchy-storage-node:latest}
    restart: unless-stopped
    network_mode: "service:tor"
    depends_on:
      tor:
        condition: service_healthy
    # チェーンが別ホストなので torsocks で包む (--chain-url が .onion)
    entrypoint: ["torsocks", "/usr/local/bin/anarchy-storage-node"]
    command:
      - --config=/etc/anarchy/storage.toml
      - --data-dir=/data
      - --rpc-port=3039
      - --public-url=${STORAGE_PUBLIC_URL_BASE:?tor の anarchy-storage/hostname から設定してください}:3039
      - --chain-url=ws://${CORE_CHAIN_ONION:?core の chain onion を設定してください}:9944
    volumes:
      - storage10-data:/data
      - ./torsocks.conf:/etc/tor/torsocks.conf:ro
      - ./storage-10.toml:/etc/anarchy/storage.toml:ro

volumes:
  tor-data:
  storage1-data:
  storage2-data:
  storage3-data:
  storage4-data:
  storage5-data:
  storage6-data:
  storage7-data:
  storage8-data:
  storage9-data:
  storage10-data:
