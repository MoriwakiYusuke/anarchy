# infra/deploy/storage-only/.env のテンプレート。cp して埋めること (.env は gitignore 済み)。

# このホストの tor が生成した onion。
#   docker compose exec tor cat /var/lib/tor/anarchy-storage/hostname
STORAGE_PUBLIC_URL=http://CHANGE_ME.onion:3030

# 接続先チェーンの onion。このホストにはチェーンが無いので必須。
CORE_CHAIN_ONION=CHANGE_ME.onion

# ANARCHY_STORAGE_IMAGE=ghcr.io/moriwakiyusuke/anarchy-storage-node:latest
